{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npx prisma generate && node scripts/migrate.js && next start",
    "restartPolicyMaxRetries": 5,
    "restartPolicyType": "on_failure"
  }
}
